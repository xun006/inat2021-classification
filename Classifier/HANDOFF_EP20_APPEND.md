
---

## 2026-09-26 — Stage1-Protocol-20：统一 20 epoch 正式实验

目的：按用户要求将正式实验预算从 30 epoch 改为 20 epoch，同时统一四组 Loss 的训练与选择流程。

设计决策：epochs=20；lr_warmup、l3_warmup、l3_ramp 均为 2，保持原各占总预算 10% 的比例。学习率前两轮线性预热，第 2 轮末达到峰值，其后 cosine 衰减至第 20 轮末为 0。组合 Loss 的 L3 在第 1–2 轮关闭，第 3 轮权重 0.5，第 4–20 轮为 1。两组基线全程 L3 权重为 0。batch=32、峰值 LoRA/head LR=1e-4/1e-3、模型、数据、增强、优化器、loss reduction、margin、lambda3 等保持不变。

严谨性约束：四组 Loss × seeds 42/43/44 均完整训练 20 轮，相同 seed 共享初始化和数据顺序策略。以验证 Top-1 选 best epoch，再综合分类与检测指标比较，不用测试集调参，不为个别 Loss 临时延长或缩短训练。20 轮是预算约束，不预设已经收敛。新实验从 PlantCLEF 权重重新初始化，不从旧 30 轮 checkpoint 续训；现有配置一致性检查仍会拒绝该种混用。输出隔离到 Classifier/outputs/ep20/，旧结果保留。

代码变更：config.json 更新四个轮数参数；run.py 将既有 LR 公式提取为 learning_rate_factor，便于检验调度端点，不改变公式；test_stage1.py 增加正式 20 轮配置、四组 L3 权重、LR 预热/中点/终点及单调性回归测试；README 更新新协议、命令、输出目录及旧 config.local.json 不随拉取更新的说明。

运行验证：本地执行完整 8 项 CPU 测试，包括新增 20 轮调度测试、Namespace 权重加载、LoRA 训练/恢复一致性和导出链路。没有运行真实 20 轮服务器实验，没有新增准确率结论。

问题与下一步：服务器拉取本次提交，使用新 config.json 与独立输出目录，从头运行 L1/L2 后再跑组合、多 seed。若仍使用本地私有配置，务必同步四个轮数参数为 20/2/2/2。正在运行的旧进程不会因拉取代码而自动切换实验配置，需结束旧进程后单独启动新方案。
